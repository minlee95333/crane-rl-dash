node_modules/
__pycache__/
*.pyc
*.pyo
dashboard_server.log
dashboard_server.pid
server.log
python_mappo/dashboard_runs/
python_mappo/auto_reward_runs/
rl_trainer/dashboard_runs/
rl_trainer/auto_reward_runs/
# 플레이 데이터와 IRL 산출물 — 로컬에서 다시 만들어지는 실행 결과물이다.
# 참여자 이름·이메일이 들어가므로 절대 커밋하지 않는다.
human_plays/
human_sessions/
human_preferences/
irl_priors/
# IRL 분석 출력 — 스텝 캐시에 참여자 id가 들어 있고, 리포트는 파생물이다.
game_irl/reports/
python_mappo/irl_priors/
# 로컬 계정 저장소 — 비밀번호 해시가 들어 있다. 절대 커밋 금지.
auth_data/
# 로컬 환경설정
.env
.env.local
.env.*.local
# 발표 자료 초안
*.pptx
