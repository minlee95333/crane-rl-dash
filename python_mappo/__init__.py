"""Backward-compatible package for the old ``python_mappo`` module path.

Active source code now lives in:
- ``crane_core`` for shared crane environment/scoring/scenarios
- ``crane_db`` for local file storage
- ``rl_trainer`` for MAPPO training and reward search
- ``game_irl`` for human-play and IRL workflows
"""
