# CLAUDE.md

이 파일은 Claude Code가 이 저장소에서 작업할 때 참고하는 프로젝트 컨텍스트입니다.

---

## 디자인 컨텍스트 (Design Context)

> Source: `/design-for-ai:design` 세션 (Design for Hackers 기반)
> 결정일: 2026-06-09

### 1. 목적 (Purpose Statement)

**크레인 양중 스케줄링 퍼즐.** 사람이 직접 풀어 남긴 행동 데이터로 MAPPO 보상함수의 7차원 학습 보상 계수를 표준 MaxEnt IRL로 역추정한다. 플레이는 ~1분, 데이터는 연구자에게 가치, 화면은 두 종류의 사용자에게 동시에 신뢰를 줘야 한다.

**사용자가 느껴야 할 감정:**
- 처음 들어왔을 때: "이건 가벼운 게임이 아니라 진짜 현장 시뮬레이션이다"
- 풀고 있을 때: "내 선택이 데이터로 의미 있게 쓰인다"
- 제출 후: "내가 무엇을 잘했고 무엇을 놓쳤는지 정확히 본다"

### 2. 청중 (Audience) — 분기 디자인

같은 페이지에서 같은 컴포넌트가 두 톤 사이를 부드럽게 전환되어야 함. 분기 트리거: `tier` 선택 (튜토리얼/표준/전문가) + `signupRoleKind` (일반/학생/전문가).

| 트랙 | 페르소나 | 핵심 needs | 시각 언어 강조 |
|---|---|---|---|
| **튜토리얼·일반인** | 김선영, 27, 비전공자. "한 번 풀어보고 친구한테 공유해야지." | 진입장벽 낮음, 명확한 라벨, 짧은 한 판, 재미 | Stripe 친근함 일부 — 긴 본문 라벨, 명확한 CTA, 단계 안내 |
| **전문가·연구자** | 박정훈, 45, 현직 양중 계획자 / 이연수, 33, RL 연구자 | 정보 밀도, 모노 숫자, 빠른 탐색, 데이터 신뢰성 | Bloomberg 정보 밀도 + Linear 모노 미니멀 — 표, tabular-nums, 키보드 |

### 3. 미학 방향 (Aesthetic Direction)

> **3 단어: "산업 안전 · 작업장 · 장난 금지" (Industrial Safety)**

| 차원 | 결정 |
|---|---|
| **메타포** | 무전기 LCD · 헬멧 스티커 · 작업표지판 · 안전 게시판 |
| **팔레트** | 강철 어두움 `#1c1f24` · 안전 앰버 `#f59e0b` · 경고 노랑 `#fef3c7` · 헬멧 흰 `#e2e8f0` · 정지 빨강 `#dc2626` |
| **타입** | Pretendard (한국어 본문/헤딩) + JetBrains Mono (모든 숫자·식별자) |
| **타입 척도** | 3:4 척도 `12 · 14 · 16 · 21 · 28 · 37` (px). 11/13/15 폐기. |
| **숫자** | `font-variant-numeric: tabular-nums`, mono, 강조 — "Bloomberg 터미널 정확도" |
| **모서리** | 작은 반경(6/10/16). 둥근 카드 만능 폐기. 직사각형 + 두꺼운 경계선 = 표지판 메타포 |
| **그림자** | 색조-시프트 (차가운 `rgba(15,23,42,.6)`). 3단 elevation 계층 (`--shadow-1/2/3`) |
| **모션** | 짧고 기능적 (120~180ms ease-out). 펄스는 한 번에 하나만(상태 머신) |
| **여백** | 비례 토큰 `--s1:4 --s2:8 --s3:12 --s4:16 --s5:24 --s6:36 --s7:54` |

**핵심 차별화**: 일반 AI 다크 대시보드(cyan-on-dark + 글래스모피즘 + 카드 만능)에서 **앰버-on-스틸**로 이동. 색 결정이 산업 안전 기호 체계에서 옴 (앰버=주의, 빨강=정지, 노랑=경고, 흰=명령).

### 4. 참고 (References) & 안티-참고 (Anti-references)

| 무엇을 가져올까 | 무엇을 버릴까 |
|---|---|
| **Bloomberg 터미널** — 모노 숫자, 표 가독성, 색 분류 | ❌ Bloomberg의 형광 청록 과부하 (분석 화면에 빨강 금지 — Ch 9) |
| **Linear** — 미니멀 인포 밀도, 작은 텍스트 위주, 키보드 친화 | ❌ Linear의 보라 그라데이션 (← AI tell) |
| **Stripe** — 교육적 친근함, 긴 본문 라벨, 일관된 미세타이포 | ❌ Stripe의 파스텔 일러스트 (이 도메인에 안 맞음) |
|  | ❌ 일반 AI 대시보드 전반: cyan-on-dark · 카드 속 카드 · 글래스모피즘 · 보라→파랑 그라데이션 · system-ui 디폴트 · 네온 글로우 텍스트 |

### 5. 제약 (Constraints)

| 제약 | 영향 |
|---|---|
| **단일 HTML, 외부 CSS 금지** | 모든 토큰은 `:root` CSS 변수. 컴포넌트는 단일 `<style>` 블록에. |
| **외부 폰트 CDN 허용** | `<head>`에 Pretendard + JetBrains Mono `<link>` 추가 가능. |
| **WCAG AA 색 대비** | 본문 4.5:1, 큰 텍스트 3:1. 새 팔레트 전수 검증 필요. 앰버 `#f59e0b` on `#1c1f24` ≈ 5.2:1 ✓. |
| **기존 게임 컴포넌트 유지** | 캔버스, 후보 버튼, 튜토리얼, 모달, 인증 등의 구조/동작은 그대로. **시각적 토큰만 교체**. JS/HTML 구조 변경은 최소. |
| **한국어 우선** | Pretendard 우선, JetBrains Mono는 숫자/ID 전용. 한·영 혼용 시 자간(letter-spacing)으로 균형. |

### 6. 진행 가이드 (Next Steps)

디자인 작업이 필요할 때 이 순서로 진행:

1. **`/design-for-ai:color`** — 산업 안전 팔레트를 색상환(앰버 기반 split-complementary)으로 정형화 + WCAG AA 검증 + 색조-시프트 그림자 계층 도출.
2. **`/design-for-ai:fonts`** — Pretendard + JetBrains Mono 페어링을 `<head>`에 주입 + 3:4 척도를 CSS 변수로.
3. **`/design-for-ai:flow`** — 경쟁 펄스 상태 머신화, 글래스모피즘 제거, `clamp()` 유동 타입, 모바일 적응(축소 아님).
4. **`/design-for-ai:brand`** — 카드 만능 제거, 이모지-아이콘 폐지(아이콘 시스템 도입), 정체성 마이크로카피.
5. **`/design-for-ai:hone`** — 마지막 원칙별 패스.

### 7. 회피 목록 (Anti-patterns — 이 프로젝트에서는 금지)

- `--accent:#38bdf8` 같은 cyan-on-dark 강조색 (← 현재 코드)
- `backdrop-filter: blur(...)` 글래스모피즘
- `linear-gradient(135deg, slate, violet)` 보라→파랑 그라데이션
- `system-ui` / `Roboto` / `Inter`를 본문 폰트로 사용 (Pretendard 강제)
- `border-radius: 14px` 만능 둥근 모서리
- 순흑 그림자 `rgba(0,0,0,...)` (차가운 색조 시프트 사용)
- 동시 펄스 애니메이션 (`candPulse` + `needsPickPulse` + `tutorialPulse` 동시 실행)
- 11/13/15px 같은 척도 외 폰트 크기
- 헤딩 색상 `#bfdbfe` (파랑 — 링크 관습과 충돌)
- 색상만으로 상태 전달 (`.ok` / `.warn` / `.bad`에 아이콘·텍스트 동반 필수)
